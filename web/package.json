{
  "name": "eberban",
  "version": "0.0.0",
  "type": "module",
  "description": "eberban language main library",
  "author": "mia entropy",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/eberban/eberban"
  },
  "scripts": {
    "task-vite-build": "tsc && vite build src --outDir ../dist --emptyOutDir",
    "task-vite-build-to-deploy": "tsc && vite build src --outDir ../..",
    "dev": "vite src",
    "build": "run-s build-peggy task-vite-build",
    "build-to-deploy": "run-s build-peggy task-vite-build-to-deploy",
    "preview": "vite preview src --outDir ../dist",
    "test": "vitest --reporter=dot",
    "build-peggy": "npx peggy ./src/grammar/eberban.peggy -o ./src/grammar/eberban.peggy.js --cache --format es"
  },
  "dependencies": {
    "esbuild": "^0.25.8",
    "preact-parser": "^1.3.7",
    "preact-render-to-string": "^6.5.13",
    "store2": "^2.14.4"
  },
  "devDependencies": {
    "@modyfi/vite-plugin-yaml": "^1.1.1",
    "@preact/preset-vite": "^2.10.1",
    "npm-run-all": "^4.1.5",
    "peggy": "^5.0.6",
    "typescript": "^5.8.3",
    "vite": "^6.3.6"
    "vitest": "^3.1.1"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.9.5"
  }
}
